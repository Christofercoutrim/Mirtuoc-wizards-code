// if e else
// if = se (isto acontecer){ isso aqui acontece}
// else = em vez (isto acontecer) {}

// = receber valor
// == NUNCA USAR
// === definir se é completamente igual

// > maior que
// < menor que
// >= maior ou igual
// <= menor ou igual

// ! não existe (quando não tem valor)

null // não existe
undefined // não é definido então não existe

// && e
// || ou

//-------------------------------------------------------------------------
//funções
// ++ acrecentar um valor

//=-=-=-

let valorpreedefinido = valor 

acontecimento aleatorio(0, 1)

function acontecimento aleatorio(recebeuAtaqueFatal, naoRecebeuAtaqueFatal){
   if(recebeuAtaqueFatal = 0){return `Você morreu`}
}

//--------------------------------------------------------------------
//se o Itachi for lutar ele ganha de qualquer um
//batalhar uma comparação primeiro ataque de cada personagem
//batalha definitiva ambos personagens até a morte de algum chegue a zero de vida
//lista de personagens citadas pelo Lucca

